import { Time } from '@angular/common';

export class TimeDetail {
    currentTime: Time;
    timeInterval: number;
    weekday: string[];
    // color: string;
    selectedTime: boolean;
    avalibaleTime: boolean;
    locationInfo: string;
} 


