import { SimplifyOrgPipe } from './simplify-org.pipe';

describe('SimplifyOrgPipe', () => {
  it('create an instance', () => {
    const pipe = new SimplifyOrgPipe();
    expect(pipe).toBeTruthy();
  });
});
