import { ColumnTitleFormatPipe } from './column-title-format.pipe';

describe('ColumnTitleFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnTitleFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
