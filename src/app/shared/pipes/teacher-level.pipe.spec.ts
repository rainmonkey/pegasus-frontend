import { TeacherLevelPipe } from './teacher-level.pipe';

describe('TeacherLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new TeacherLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
