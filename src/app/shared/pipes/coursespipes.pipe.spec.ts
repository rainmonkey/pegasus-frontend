import { CoursespipesPipe } from './coursespipes.pipe';

describe('CoursespipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CoursespipesPipe();
    expect(pipe).toBeTruthy();
  });
});
