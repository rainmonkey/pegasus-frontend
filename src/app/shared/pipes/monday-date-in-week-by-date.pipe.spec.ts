import { MondayDateInWeekByDatePipe } from './monday-date-in-week-by-date.pipe';

describe('MondayDateInWeekByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MondayDateInWeekByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
